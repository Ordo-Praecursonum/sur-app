# Sur app

Sur is a protocol allowing to differentiate AI generated content by human generated content by creating a data provenance infrastructure.
It uses cryptographic functions to ensure confidentiality and traceability.

